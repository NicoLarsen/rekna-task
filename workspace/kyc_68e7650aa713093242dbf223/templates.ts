// Document Templates registry - single source of truth for all document templates
// When creating a new document template, only 'name' and 'fileType' can be set
export const templates: DocumentTemplateEntry[] = [
];
